The previous workbook program caught a processing error but still printed complete progress. That evidence is invalid.

Repair the recorded program with one complete bounded source action. Do not compare read-only `max_row` or `max_column` after `reset_dimensions()`. Consume the header and data from one iterator. Count a workbook as done only after every requested worksheet was read successfully; a caught error must print concise stderr, keep `VAULT_PROGRESS_COMPLETE=0`, and exit nonzero.
