The previous attempt did not produce a verified GFM table with complete workbook coverage. It may have produced plain text or inconsistent Markdown columns, split a source line around Markdown pipe characters, emitted an invalid escape-sequence warning, or called `reset_dimensions()` on a normal OpenPyXL `Worksheet`.

Replace it with one fresh, small program. Use columns `["Source", "Sheet", *header]` and results `[path, sheet.title, *row]`, never one nested `Data` cell. Construct the table separator with `chr(124)`. Define `emit(cells)` as `print(sep + " " + (" " + sep + " ").join(str(value).replace(sep, " ") for value in cells) + " " + sep)`. Replace embedded separators with a space instead of a backslash escape. Do not call `replace` on backslash characters. Emit columns, then `["---"] * len(columns)` for one dash-based separator row, then `emit(result)` for every result. If a result is `[path, sheet.title, *row]`, never iterate `result[2]`: that is one scalar cell and splits strings into characters. Use the whole `result`, or `result[2:]` only when intentionally omitting source metadata. Never multiply `sep`, slice results, or handwrite a pipe separator. Count printed data rows with an integer variable. Print no prose, totals, or blank content outside the table, and include the required workbook coverage markers.

For missing or extra rows, build the complete sorted `os.walk` corpus with `name.casefold().endswith(".xlsx")`. Iterate `for sheet in workbook.worksheets`. Never build `rev_cols` from target words in headers. Locate `flow_index` from `cash_flow`/`direction`/`flow` and `text_indexes` from `category`/`description`/`note`; test row values. Require incoming/inflow/credit plus revenue/sale/customer-payment/received when flow exists, otherwise test the full row. Preserve every source column and print progress once after all paths.

Use `load_workbook(..., read_only=True, data_only=True)` before calling `reset_dimensions()` when scanning source workbooks with collapsed dimension metadata. When reopening the generated `/workspace` workbook normally, do not call `reset_dimensions()`.
