The previous attempt did not produce a verified GFM table with complete workbook coverage. It may have produced plain text or inconsistent Markdown columns, split a source line around Markdown pipe characters, emitted an invalid escape-sequence warning, or called `reset_dimensions()` on a normal OpenPyXL `Worksheet`.

Replace it with one fresh, complete, small program. Construct the table separator with `chr(124)` and join cells with that variable; replace embedded separators in cell text with a space instead of a backslash escape. Do not call `replace` on backslash characters and do not put a backslash string literal in the generated source. Print exactly one header row, one dash-based separator row, and one row per result; never print another separator inside the data loop. Count printed data rows with an integer variable instead of calling `len` on an OpenPyXL row iterator. Print no prose, totals, or blank content outside the table. Keep every source-array item as one complete Python line and include the required workbook coverage markers.

Use `load_workbook(..., read_only=True, data_only=True)` before calling `reset_dimensions()` when scanning source workbooks with collapsed dimension metadata. When reopening the generated `/workspace` workbook normally, do not call `reset_dimensions()`.
