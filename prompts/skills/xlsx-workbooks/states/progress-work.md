Current required phase: perform bounded workbook work.

Name the selected strategy in the summary before generating at most 80 complete source lines. Use one direct loop that preserves the streaming, checkpoint, progress, and coverage requirements.

A successful execution must leave stderr completely empty. Do not print file discovery, status, warnings, or debug text there; any stderr on exit code 0 is unverified.
