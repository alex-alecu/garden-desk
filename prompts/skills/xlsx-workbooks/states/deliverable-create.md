For a new XLSX import both `Workbook` and `load_workbook`, create with `Workbook()`, save once, then reopen with the function `load_workbook(output_path, data_only=False)`. Never call `load_workbook` as a method on a `Workbook` instance. For an edit load the real source in normal mode with `data_only=False`, change only requested cells, and save a separate workspace output. Verify exact rows, formulas, styles, merges, panes, filters, sheet order and visibility before declaration. After the reopened values pass, close it and print exactly `VAULT_PROGRESS_DONE=1`, `VAULT_PROGRESS_TOTAL=1`, and `VAULT_PROGRESS_COMPLETE=1`; never print bare marker names.
