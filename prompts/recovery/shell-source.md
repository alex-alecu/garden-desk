The most recent shell proposal embedded a Python or Node program, so it was not executed.

Submit that program as a fresh Python or Node source action of at most 64 complete lines instead; Vault Desk writes the complete source to a workspace file and executes it without shell quoting.
