The most recent proposal was rejected before execution because a quoted string was left open at a line break. A single- or double-quoted string cannot span a line in Python or Node.

This usually happens when a newline is written directly inside the quotes instead of as an escape. Inside the string, write a newline as the two characters backslash and `n`, or close the string on the same line.

Do not resubmit the same source. Correct only the affected line, keep the rest of the complete program unchanged, and submit the corrected version.
