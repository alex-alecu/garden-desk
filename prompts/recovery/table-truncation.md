The proposed program was rejected before execution because the task requires every result in a table, but it would omit fields, put multiple fields in one `Data` cell, build malformed Markdown columns, create an unrequested workbook, or slice part of a path, cell, row, or result set.

Submit one fresh complete program of at most 80 short lines without `[:N]`, `[-N:]`, `.slice(0, N)`, fixed-width clipping, abbreviations, or placeholders. Preserve every selected field and full value. Use one flat discovery-and-processing loop and avoid explanatory comments. The requested output mode is literal: when the task says the table must be here or in chat, print one valid GFM table and never save a workbook. Otherwise, if the complete table cannot fit the response limit, write every row and column to one verified XLSX under `/workspace` instead.
