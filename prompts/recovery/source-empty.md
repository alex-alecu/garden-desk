The latest source inspection produced no evidence. Do not repeat the requested output label as a literal search and do not use another regex.

Use one fresh source-only program of at most 40 lines to read the discovered candidate as UTF-8 and print its complete bounded content unchanged. The next turn translates the observed declaration into the requested `LABEL=value` even when its identifier differs.
