# Headless Stress Testing

## How the headless runner works

The cross-platform headless paths run the shipped backend without the desktop UI. `pnpm test:stress:m3:small` starts Vault Core and its current-user daemon, verifies the pinned `gemma-4-12b-it-qat-q4_0` model, and uses the generated platform helper and agent image to run work inside the real no-NIC guest. `pnpm test:m3:macos` and `pnpm test:m3:windows` exercise the broader platform-specific M3 contracts.

The runner creates its state and realistic fixture roots under the platform temporary directory, grants a selected folder through `folders.add`, creates sessions, starts work through `agent.start`, and polls `agent.get` until every run is terminal. The selected folder is mounted live and read-only at `/source`; only the bounded guest `/workspace` is writable. Current profiles ask for realistic PDF, Word, Excel, and cross-format management reports from mixed business corpora. Expected facts stay outside production prompts. Every generated deliverable is materialized and reopened from its real bytes, either through a deterministic format check or an independent verification session with the corresponding local document library. Response text or a succeeded state alone never passes. The runner also tests generic continuation markers, policy rejections, routing negatives, Romanian skill triggers, concurrent sessions, immutable traces, audit, cleanup, and hidden-answer terminal evidence. Complete ignored evidence is written under `packages/eval/.generated/stress/`.

Task-specific evaluations use an ignored TypeScript runner under `packages/eval/.generated/` with the same Core, daemon, CLI RPC, real Gemma worker, and no-NIC guest. Expected answers must remain outside the model prompt, and success must come from current execution evidence rather than response text or historical runs alone.

## Latest results

### Realistic file-manipulation and recovery certification - 2026-08-11

- Why the prior stress evidence was too weak: several cases accepted a succeeded agent run, a declared filename, or extracted label text without proving the requested edit, structure preservation, page order/rotation, workbook coverage, direct-chat shape, or current artifact freshness. The plain-language 36-workbook table case was also absent from the authoritative default sweep, while the oversized-table fixture had only one worksheet per workbook and could not catch worksheet counts being reported as workbook coverage.
- Stress oracles now reopen the real materialized bytes independently. XLSX checks verify exact cell facts plus formulas, hidden sheets, merges, panes, filters, styles, forbidden old values, and full row coverage. DOCX checks inspect visible document/header/footer text and raw package structure. PDF checks inspect extracted page text in order, page rotations, and metadata. Deliverables whose underlying `LABEL=value` facts change are invalidated until recreated and reopened, so a stale earlier artifact cannot satisfy finalization.
- New realistic manipulation cases are first-class sequential and three-way concurrent work: `xlsx-edit` changes only two requested cells while preserving formulas, styles, a hidden audit sheet, merge, freeze pane, and filter; `docx-edit` replaces one phrase, adds a real heading and paragraph, and preserves body/table/header/footer/page setup; `pdf-merge` preserves ordered page content, rotates only the final page, and verifies `/Title`. The default suite requires `maximumRunning=3` and independently verifies all three concurrent outputs.
- XLSX analysis stress is stricter. `excel-chat-path-list` hides 36 marker/amount pairs across nested uppercase-extension workbooks and requires one valid complete GFM table here, exact 36/36 coverage markers, every field in its own column, and no artifact. `oversized-table-result` hides 2,000 rows across four workbooks with two worksheets each and requires one reopened overflow XLSX with all rows while DONE remains 4 workbooks, not 8 worksheets. Romanian aggregation still proves exact `TOTAL_AVANS=12009` across multi-sheet inputs.
- Production recovery is skill-driven rather than format policy in generic core. Active `SKILL.md` frontmatter owns source-rejection and removable-import rules; the generic loop only applies typed active-skill rules. The XLSX skill now supplies bounded direct-table, aggregate-analysis, multi-format-analysis, overflow-artifact, progress-placement, row-serialization, and certified-API shapes. Source/schema bounds agree at 80 lines for initial workbook work and 64 for malformed recovery. Core rejects malformed progress, progress inside loops, numeric table truncation, scalar-character serialization, unrequested direct-chat artifacts, stale deliverables, and known active-skill API misuse before guest execution.
- The local DOCX, PDF, and XLSX skills were compared with public best-in-class patterns and official library contracts before revision. Research-derived references: [Anthropic document skills](https://github.com/anthropics/skills), [openpyxl optimized modes](https://openpyxl.readthedocs.io/en/stable/optimized.html), [python-docx API](https://python-docx.readthedocs.io/en/latest/), [pypdf user guide](https://pypdf.readthedocs.io/en/stable/user/index.html), and [ReportLab user guide](https://docs.reportlab.com/reportlab/userguide/). No new runtime dependency or cloud path was added; shipped behavior remains the pinned local libraries in the no-NIC guest.
- Final physical Apple-silicon default sweep: `small-2026-08-11T10-50-53.815Z.json` returned `small_stress_passed`. All 13 sequential and three concurrent results passed with real pinned `gemma-4-12b-it-qat-q4_0`, real no-NIC guests, immutable traces, exact hidden facts, independently reopened outputs, one context compaction, three repeated context compactions, and `maximumRunning=3`.
- Final focused-only cases also passed on the exact final implementation: PDF report `small-2026-08-11T11-16-25.816Z.json`, Excel report `small-2026-08-11T11-18-55.726Z.json`, and the 24-workbook/31-file multi-format continuation report `small-2026-08-11T11-23-10.727Z.json`. Each produced the requested artifact and passed independent byte-level verification of all four hidden business facts.
- Final canonical `pnpm test:m3:macos` returned `classification: certified`: real Python and Node each completed two executions with one artifact at 131,072 tokens; the no-NIC guest blocked host paths, IPv4/IPv6, DNS, VSOCK, Unix sockets, socket pairs, package managers, credentials, and out-of-scope writes; document probes, cancellation, timeout, output limiting, process/memory/quota limits, crash containment, symlink rejection, persistence, folder revocation, session deletion, and two overlapping VMs passed.
- Final `PATH=/Users/alex/.cargo/bin:$PATH pnpm verify` passed the 300-line source limit, Biome, TypeScript, 112 unit files with 536 tests passed and one declared skip, two native tests, sidecar/resource verification, desktop production build, Cargo formatting/clippy, and macOS/Windows helper build stages. This is physical macOS and local cross-platform build evidence only; no new physical Windows agent, installed-app, or signing result is inferred.

### Plain-language Excel search routing fix - 2026-08-10

- Symptom: the exact user task `Search all excel files in current folder for revenue that came into the business and return the results in a nice table here` never processed the workbooks. On current source the real pinned Gemma agent issued one shell `find /source -type f -iname '*.xlsx'`, then looped rejected duplicates and ended `agent_stalled_duplicate` with no Python, no table, and no progress markers.
- Root cause (production): generic declarative skill routing in `packages/core/src/agent/prompt-library.ts` only activates a format skill when a recognized action verb appears within 120 characters of a format keyword, and the `FORMAT_ACTION` vocabulary omitted `search`/`caută`. The exact prompt therefore left `xlsx-workbooks` inactive, so the model fell back to shell discovery instead of a bounded openpyxl program. The hash-pinned August 4 release bundle (XLSX prompt SHA-256 `a845336fc21d3a80ca61cc6d8a9ae5463ceaf6bba334a6bdc641e78bc81ddc16`, sidecar SHA-256 `1bf22434e3ef8dffa150194788e25bc90a24db2916c855d64e8d3c900bdbd09b`) routed straight to XLSX and did not stall, which localized the regression to current-source routing rather than the fixture.
- Fix (production, four parts): added `search`, `caută`, and `cauta` to the generic `FORMAT_ACTION` verb set; and, in the format-specific `xlsx-workbooks` skill, pinned discovery to the read-only `/source` mount (so "current folder" is not read as `.`), required a separate `print()` per progress marker (adjacent f-strings dropped the newlines), and required each source field to occupy its own full-value table column via a concrete `values_only` row-render idiom (banning row-into-one-cell, fixed-width `[:N]` slicing, and dropped amounts). The skill was kept size-neutral so the certified 8K three-format prompt floor still fits.
- After the fix, `traceError` is null on every physical run: the XLSX skill activates, discovery targets `/source`, execution is Python-first with no shell, and complete `VAULT_PROGRESS_DONE=36`/`TOTAL=36`/`COMPLETE=1` markers print. The shell-first stall regression no longer reproduces. The tracked `excel-chat-path-list` case reached two consecutive clean passes (`small-2026-08-10T14-04-26.087Z.json`, `small-2026-08-10T14-06-17.773Z.json`): all 36 marker/amount pairs on GFM rows, exactly two executions, no artifact.
- Residual limit: the inline 36-row table step still varies run to run at temperature 0 on this local GPU. Later runs showed the model writing its own buggy formatters (dict-indexing a positional row tuple → `list indices must be integers`, empty-pipe tables, or exceeding the execution budget), so the case is classified `small_stress_limit_found` rather than a guaranteed pass. This is the same model-quality variance already documented for `oversized-table-result` and `context-compaction`, not the routing regression, which is fixed. The case's `maxExecutions` was corrected from an unrealistic 2 to 5 (matching the peer `excel-row-filter` case) while every correctness gate (all 36 amounts, XLSX-first routing, `/source` Python discovery, no shell, complete markers, no artifact) stays strict.
- Reconciliation: the ignored `packages/eval/.generated/reproduce-f9dec141.ts` runner, which previously expected the (incorrect) package stall, now verifies the packaged bundle's real correct direct-XLSX behavior (succeeded run, active XLSX first prompt, Python-first, no shell, complete markers, all 36 rows).
- Gates: focused routing/reporting units pass (`prompt-library.test.ts` now asserts the exact task and a Romanian variant route to `xlsx-workbooks`); `PATH=/Users/alex/.cargo/bin:$PATH pnpm verify` passed (498 unit tests, one skip, two native tests, lint, typecheck, source limits, Rust/helper checks, sidecar validation, desktop build); and the canonical physical `pnpm test:m3:macos` returned `classification: certified` (real Python and Node in two executions with artifacts, no-NIC guest isolation, docx/pdf/xlsx probes, limits, persistence, cancellation, two overlapping VMs at 131,072 tokens). macOS evidence only; Windows behavior is not inferred here.

### macOS 128K post-Windows-PR reliability - 2026-08-09

- After the Windows-authored context-compaction PR series merged, `pnpm desktop:dev` failed on macOS because the sidecar bundle resolves `@vault/shared` through its `vault-runtime` export condition (`packages/shared/dist`) but nothing rebuilt that `dist`, so the new `AgentSessionSummarySchema` and `MAX_ANCHORED_SUMMARY_CHARACTERS` exports were missing. `build-sidecar.ts` now compiles `packages/shared` before the core bundle, so a fresh checkout with new shared exports starts without a manual `tsc`. Deleting `packages/shared/dist` and rebuilding reproduces and confirms the fix; `prepare:dev` then bundles Core and the full Tauri desktop starts.
- The physical `pnpm test:stress:m3:small` run on Apple silicon (51 GiB, 131,072-token tier, real pinned Gemma, no-NIC guest) reproduced a deterministic agent stall on `context-compaction`. With temperature 0 the same task prompt reliably produced a Python `IndentationError` on the first attempt; the model then generated a correctly indented repair, but it carried a trailing hallucinated schema-field token (`skills_requested_none`), which the invalid-program guard rejected. Every repair was rejected the same way and the run ended `agent_stalled_duplicate`. Two identical runs failed byte-for-byte (`small-2026-08-09T08-32-34.305Z.json`, `small-2026-08-09T09-05-09.798Z.json`), and the same case passed on this machine on 2026-08-07 (`small-2026-08-07T04-42-36.032Z.json`), so this was a real recovery regression rather than model noise.
- Fix: `normalizeSourceItems` now strips trailing lines composed only of bare identifier tokens (with no punctuation or leading keyword) before delimiter balancing. A repaired program keeps `main()`, `pass`, `return`, real trailing calls, and quoted-string statements, while hallucinated `skills_requested_none` / `command_args_none` debris is removed so a valid repair is accepted. Focused coverage added in `loop-source-recovery.test.ts`; `pnpm test` passed 487 tests with one declared skip, `pnpm test:native` passed, and typecheck, lint, and source limits passed. No model, context, execution, timeout, VM, workspace, memory, or isolation limit changed.
- Re-run after the fix (`small-2026-08-09T09-51-01.386Z.json`): the `context-compaction` stall is gone. The run recovered from the first-attempt crash, one automatic compaction fired, and it returned the exact `COMPACTION_TOTAL=38687`; it now trips only the strict `maxExecutions: 1` harness assertion because the model's first attempt still crashes and needs one repair. `excel-row-filter`, `invalid-document`, `romanian-task`, `no-skill-direct`, `terminal-discovery`, and `repeated-context-compaction` (three compactions, exact stage totals) all passed. `oversized-table-result` remains a separate model-quality limit: Gemma degenerates mid-program into repeated and multilingual junk tokens (`الي`, `இச`, `alfdf…`) inside large source, which is not the clean trailing-debris case, so it still ends `agent_stalled_duplicate`. The concurrent `excel-row-filter` overlap ended `structured_tool_call_required`. The suite classification stays `small_stress_limit_found` on these residual model-quality limits.
- Canonical gate after the fix: physical `pnpm test:m3:macos` returned `certified`. Real Python and Node each completed in two executions with one artifact and two clean attempts (no stall retries), DOCX/PDF/XLSX document probes passed, and guest isolation (host-path, network, IPv6, DNS, vsock, unix, socketpair, package managers, credentials), execution limits, timeout, output truncation (`resource_limit`), process/memory/quota limits, crash handling, symlink rejection, workspace persistence and repair, cancellation, folder revocation, session deletion, and two overlapping VM lifetimes all passed at the 131,072-token tier.
- Anchored long-chat after the fix: physical `pnpm test:stress:m3:context-session` returned `context_session_passed` (`context-session-2026-08-09T10-17-17.137Z.json`). All seven turns succeeded at the 131,072-token allocation, three distinct evolving anchors reached later agent prompts (turns 3–7 `anchored: true`), no required terms were missing, and the final response recovered the exact review style, unresolved item, next move, offline preference, and delivery format. This exercises the anchored session-summary path (`AgentSessionSummarySchema`) whose missing compiled export first surfaced the `pnpm desktop:dev` start failure.

### Dynamic context turnover certification - 2026-08-08

- Prompt fitting now treats the worker's reported allocation as the authority. Focused tests exercise 8,192, 16,384, 65,536, and 131,072 tokens: the same 20,000-character observation compacts at the two smaller allocations and remains verbatim at the two larger allocations, while every serialized request stays inside its allocation after the generation reserve.
- Successful source and commands now share the allocation-derived execution-context budget with stdout and stderr. A completed 128,000-character source compacts and fits at every tested tier; failed repair source remains exact and returns `agent_context_exhausted` when the 8K tier cannot preserve it.
- Older chat now uses its anchored summary as soon as verbatim older messages exceed the current history budget. Summary generation selects the largest pending-message prefix that fits the reported allocation, advances its covered-message cursor by that prefix, and makes one fresh structured-call retry before deterministic fallback.
- The first repeated-execution fixture reached five compacted prompts but failed its final structured call after the model misread lower-case headers (`small-2026-08-08T17-17-18.860Z.json`). Three corrected fixture iterations then exposed target-column, exhausted-reader, and header-case mistakes while still proving three compacted prompts (`small-2026-08-08T17-20-09.664Z.json`, `small-2026-08-08T17-21-44.364Z.json`, and `small-2026-08-08T17-24-34.595Z.json`).
- The exhausted-reader run exposed a product continuity defect: the 24-line anchored ledger retained only the newest execution after generic record lines displaced earlier target rows. Evidence selection now reserves four ranked lines for each of the six possible executions and strongly prioritizes explicit uppercase identifiers and exact labels; an anchored build and a from-scratch build retain the same target rows.
- Final physical Windows repeated-turnover evidence passed on the dynamically reported 65,536-token allocation: exactly three Python executions, exactly three compacted traced prompts, and exact `STAGE_1_TOTAL=116224`, `STAGE_2_TOTAL=218005`, and `STAGE_3_TOTAL=319786`. Report: `small-2026-08-08T17-26-22.017Z.json`.
- The first dynamically sized chat session completed seven real-model runs and recalled all five old decisions, but all summary attempts returned prose rather than the required call, so deterministic excerpts carried continuity and the oracle correctly failed with zero anchors (`context-session-2026-08-08T17-28-30.309Z.json`). A direct service probe identified `structured_tool_call_required`; the new bounded retry then persisted the summary without changing the completed run.
- Final physical Windows chat evidence passed with 69,043-character pressure turns derived from the actual 65,536-token allocation. Seven runs all succeeded, every trace reported that allocation, three distinct evolving anchors reached later agent prompts, and the final response recovered the exact review style, unresolved item, next move, offline preference, and delivery format. Report: `context-session-2026-08-08T17-37-43.754Z.json`.
- The complete final Windows small sweep passed all eight sequential and three concurrent results in 485.8 seconds with `maximumRunning=3`. It repeated the one-compaction exact `COMPACTION_TOTAL=38687` case, the three-compaction exact stage totals, independently reopened `revenue_received_report.xlsx` and `avans_rows.xlsx`, and passed invalid-input, Romanian routing, direct response, terminal discovery, and concurrency checks. Report: `small-2026-08-08T17-41-19.392Z.json`.
- The first post-verify canonical Windows attempt completed every guest stage but could not start packaged inference because verification had cleaned its staged resources. After rebuilding, two identical runs contained the intentional one-million-byte stdout limit correctly but failed finalization with `structured_tool_call_required`. The source-discovery schema had been tested against a nonexistent `output_limit` termination while the typed guest protocol reports exit-zero output truncation as `resource_limit`, so production still removed the response branch and demanded another source action.
- The typed fix allows finalization only for nonempty, exit-zero `resource_limit` evidence; crashes, timeouts, nonzero resource failures, empty discovery, and stderr remain source-recovery-only. The final physical `pnpm test:m3:windows` returned `certified_headless`: the limit run succeeded with exactly 1,000,000 retained stdout bytes and `stdoutTruncated=true`, and Python, Node, cancellation, timeout, process, memory, quota, no-NIC security, malformed-frame teardown, persistence, document probes, and complete teardown passed.
- No model, context, generation, execution, VM, workspace, time, filesystem, network, or isolation limit increased. Real repeated-turnover and long-chat evidence is Windows 64K only; the same allocation-derived runners remain to be executed separately on physical 128K and macOS tiers.

### Anchored context continuity - 2026-08-08

- Two OpenCode-derived improvements landed after comparing its `session/compaction.ts` with ours. OpenCode runs a deterministic `prune()` over old tool output plus a model-written anchored summary that merges into `<previous-summary>`; we previously had only the deterministic half, and no continuity record once a session outgrew its verbatim history budget.
- Deterministic ledger anchoring merges each run's compacted ledger forward instead of recomputing it from every raw stream on every prompt build. Evidence and artifacts are ordered sets and warnings are keyed by execution step, so the merge is exact and adds no drift. A focused test asserts an anchored build and a from-scratch build produce identical ledgers.
- The anchored conversation summary replaces the former `N older messages remain in durable conversation history.` marker, which carried no intent, decisions, or blockers. It is untrusted continuity prose: it never carries `LABEL=value` facts, never gates completion, runs only at or above a 16,384-token context, and falls back to the deterministic marker whenever the model fails, returns nothing usable, or the session is still short.
- Catalog schema v12 stores exactly one replaceable anchor per session, removed with its session by cascade, so a long session keeps one evolving document rather than an unbounded chain.
- Focused coverage passed: anchored merge and compaction equivalence, summary guardrails, history fallback in both directions, catalog migration, and a five-turn service lifecycle proving the anchor is persisted and then merged through `<previous-summary>` on a later turn.
- `pnpm verify` passed after the change: 101 unit files and one declared skip, 476 tests passed with two declared skips, native tests, TypeScript, lint, source limits, Rust and native helper checks, sidecar and resource verification, and the desktop production build.
- Physical Windows re-evidence on the final code: focused context compaction passed in one execution with exact `COMPACTION_TOTAL=38687` and one compaction (`small-2026-08-08T15-17-54.959Z.json`); the complete small sweep passed all seven sequential and three concurrent results with `maximumRunning=3`, including independently verified `revenue_received_report.xlsx` and `avans_rows.xlsx` (`small-2026-08-08T15-19-09.777Z.json`); and `pnpm test:m3:windows` returned `certified_headless` with the refreshed guest document probe intact.
- No context, execution, time, VM, workspace, memory, or isolation limit changed. Exact values still come from durable execution records, never from summary prose.

### Windows certification cycle - 2026-08-08

- The canonical physical `pnpm test:m3:windows` gate returned `certified_headless` with the real pinned Gemma worker and x86_64 no-NIC guest. Python, Node, cancellation, timeout, 1,000,000-byte output truncation, malformed-frame teardown, live read-only source, guest workspace persistence, process and memory limits, document-library probes, and host-path and network confinement passed.
- Focused context compaction passed in one execution with exact `COMPACTION_TOTAL=38687`, a 335,045-byte three-file fixture, and one automatic compaction. Report: `small-2026-08-08T09-07-59.351Z.json`.
- The first focused oversized-table run ended as `small_stress_limit_found` after six executions and no artifact. Every generated Python attempt contained a source item at the 512-character structured-output boundary, including repeated zeroes and malformed appended text. Report: `small-2026-08-08T09-13-12.586Z.json`.
- After rejecting boundary-truncated source before guest execution, the identical case reached a clean 4/4 execution and captured `revenue_received_results.xlsx`, but Core then issued contradictory table-repair guidance instead of accepting the overflow workbook and exhausted the planning limit after five executions. Report: `small-2026-08-08T11-43-02.853Z.json`. The final fix also rejects generated entry-point programs that define `main` without invoking it and accepts a captured overflow workbook instead of requiring a duplicate chat table.
- The final identical oversized-table rerun passed in three executions and 120.258 seconds. Core captured `revenue_received_summary.xlsx`, the independent verifier reopened the real artifact bytes, and all 2,000 hidden revenue-row facts were present. Report: `small-2026-08-08T11-49-37.624Z.json`.
- The complete default Windows small sweep passed all seven sequential and three concurrent results with `maximumRunning=3`. Sequential context compaction retained exact `COMPACTION_TOTAL=38687` with one compaction; the oversized result again delivered and independently verified `revenue_received_summary.xlsx`; row filtering, invalid-document clean stop, Romanian routing, direct response, and terminal discovery all passed. Report: `small-2026-08-08T11-52-00.776Z.json`.
- Scaled sequential completed as `scaled_stress_limit_found`. The PDF run produced `scaled-policy-report.pdf` but independent verification found missing `POLICY_PAGES=100`; the 50-workbook, 10-million-row XLSX run used all six executions without a deliverable; the 50-file mixed run stopped after five executions with `agent_stalled_duplicate`. Report: `scaled-sequential-2026-08-08T12-00-22.959Z.json`. These match model and continuation limits rather than a Windows inference fallback: the retained worker evidence records the single-device 17,094,475,776-byte VRAM budget, 9,175,791,872 GPU VRAM bytes, 910,614,240 CPU RAM bytes, and the 65,536-token `windows_gpu_vram_at_most_24_gib` tier. During the run, live `nvidia-smi` samples reached 94 percent GPU compute and 83-84 percent memory-controller utilization with about 10.5 GiB total device allocation including Windows display use.
- The first scaled concurrent Windows run retained the same PDF fact miss and no XLSX deliverable, while the third cold-start cross-format run failed before inference with `agent_context_exhausted`; only two runs overlapped (`maximumRunning=2`). Report: `scaled-concurrent-2026-08-08T12-24-57.305Z.json`. The exact three-skill Gemma request was 4,162 estimated tokens against the certified 4,096-token request floor before the worker reported its 64K allocation. Generic save, reopen, and declaration guidance duplicated across the three active document skills is now compacted without dropping format-specific verification, and the exact scaled task has a floor-budget regression test before the concurrent rerun.
- The corrected scaled concurrent rerun reached `maximumRunning=3`; all three cases entered real inference and guest execution, and the cross-format cold start no longer exhausted context. It remained `scaled_stress_limit_found`: the PDF missed exact `POLICY_PAGES=100`, the XLSX artifact missed exact `MATCHING_INVOICES=500` and `INVOICE_TOTAL=12752750`, and the cross-format run completed six executions without a deliverable. Report: `scaled-concurrent-2026-08-08T12-33-15.858Z.json`. This closes the prompt-budget and scheduler regression while retaining the separate model-quality limits accurately.

### Three-skill prompt budget after merging generated-file freshness - 2026-08-07

- Merging the generated-file freshness work into this branch made a three-format request exceed the 8K prompt floor. `Create report.pdf, report.docx, and report.xlsx.` activates the DOCX, PDF, and XLSX skills together, and the merged request serialized to 4,104 tokens against the fixed 4,096-token request budget, so `generationInput` returned `agent_context_exhausted` before any inference ran. The same request fit at 14,940 prompt characters before the merge.
- Both branches were individually correct. The freshness change added the same deliverable-recreation sentence to all three document skills, and this branch added compacted task state and deliverable-fact instructions, so only the combination crossed the limit.
- The duplicated sentence is generic rather than format-specific, so it now appears once in the always-loaded system prompt instead of three times in skill bodies. Behavior is preserved for every deliverable format, including formats without a document skill, and the three-skill request fits again. No context, execution, time, VM, workspace, or isolation limit changed.


The 2026-08-06 context-turnover regression cycle split the small profile's stochastic management-report requests into directly runnable focused PDF, DOCX, XLSX, and large-corpus cases. The redundant monolithic request repeatedly kept Gemma in one constrained generation until the fixed worker timeout, while each format-specific case exercised the same hidden four-fact corpus and passed direct artifact-byte verification. The default sequential and concurrent sweeps use generic deterministic cases so a worker-generation timeout cannot prevent agent regressions from running. The scaled profile retains cross-format and large-corpus continuation workflows; no product context, inference, execution, VM, workspace, time, or isolation limit changed.

### Unbacked promise-only response recovery — 2026-08-07

- A reported local session finished as `succeeded` for `Write a short story for children in word & pdf docs` with zero executions, zero artifacts, an empty workspace manifest, and only the promise `I will create a short story for children titled 'The Brave Little Squirrel' and save it as both a DOCX and a PDF document in the /workspace directory.` The supplied debug snapshot preserved both traced turns, the exact prompts and schemas, and outcomes `accepted_skill_request` and `accepted_response`, which was sufficient to diagnose the defect without a new capture.
- Root cause was three compounding gaps: the loop accepted any `respond` decision before checking that requested deliverables existed, `requiresSourceExecution` left the `respond` branch in the decision schema because no active skill required execution evidence, and only `xlsx-workbooks` declared progress markers so DOCX and PDF deliverables had no execution-backed completion requirement. Abbreviated `word & pdf docs` phrasing also failed DOCX keyword routing, spending the entire first turn on a skill-activation round trip that emitted a duplicate `Loading the local model and planning the task.` event.
- After the fix, the identical task ran twice through the real current-user daemon, pinned Gemma worker, and no-NIC guest and succeeded both times. Traced outcomes were `rejected_unbacked_response`, `accepted_execution`, `accepted_execution`, `accepted_response`. The run persisted `Pip_and_the_Magical_Acorn.docx` at 36,974 bytes and `Pip_and_the_Magical_Acorn.pdf` at 2,089 bytes. The first guest attempt exited 1 with `crash` and the model repaired it in one further execution, so recovery remained real rather than assumed. This is macOS evidence only.
- The refused turn is now recorded rather than silently accepted: catalog schema version 11 adds the typed `rejected_unbacked_response` outcome. The first real rerun failed closed with `agent_model_failed` until that migration existed, which confirmed the constraint path rather than masking it.
- Deliverable policy stays in skill metadata through the new `produces-deliverables` flag, so `packages/core/src/agent/deliverable-completion.ts` names no document format and the generic agent architecture boundary test still passes.
- Focused coverage passed: 166 tests across 29 agent files, including a promise-only rejection, a clarifying-question rejection, schema removal of the `respond` branch on the retry turn, abbreviated Word routing, and single-emission planning messages. `pnpm verify` passed 95 unit files with 425 tests and one declared skip, two native tests, TypeScript, lint, source limits, Rust and native helper checks, sidecar validation, and the desktop production build. The canonical physical `pnpm test:m3:macos` gate returned `certified` both before and after the migration, with the 131,072-token context tier and two overlapping VM lifetimes.

### Generated-file freshness and planning recovery — 2026-08-07

- The exact reported task `Write a short story for children in a word and a pdf doc` passed through the real current-user daemon, pinned Gemma worker, and no-NIC guest. It persisted exactly `story.docx` and `story.pdf`; signatures were `504b0304` and `25504446`; an independent guest reopened both as non-empty documents; final declarations matched both artifact rows; materialized and exported bytes matched; and the 25-event audit chain verified. Report: `generated-deliverables-2026-08-07T08:00:07.619Z.json`.
- The richer hidden-fact two-file fixture also passed in two executions and three traced turns. Both artifacts contained all four hidden facts, both signatures and export bytes verified, and the 15-event audit chain passed. Report: `generated-deliverables-2026-08-07T07:44:08.088Z.json`.
- The focused XLSX row-filter oracle passed in two executions and 96.209 seconds. Independent reopening verified `avans_rows.xlsx` with all hidden matching rows and no ordinary-transfer rows. Report: `small-2026-08-07T08-00-29.416Z.json`.
- The first focused PDF report run created `management-report.pdf`, but independent byte verification correctly failed because the file contained `INVOICES=0` and `TOTAL=0.0` instead of the hidden invoice facts; `NOTES=24` and `PAGES=12` were correct. Report: `small-2026-08-07T08-02-17.869Z.json`. This exposed case-sensitive workbook-header matching; the workbook skill now requires case-insensitive normalized headers and fail-closed missing-column handling.
- The identical PDF report rerun extracted the exact hidden facts `MATCHING_INVOICES=8`, `INVOICE_TOTAL=20012.0`, `MEETING_NOTES=24`, and `POLICY_PAGES=12`, created and reopened the PDF, but Gemma failed to issue a final structured declaration after five executions. Core failed closed with `structured_tool_call_required` and persisted no artifact. Report: `small-2026-08-07T08-06-51.776Z.json`. This remains a real-model quality limit; no execution, time, context, security, or planning limit changed.
- The canonical physical `pnpm test:m3:macos` gate returned `certified` with real Python and Node artifacts, persistence, cancellation, resource limits, no-NIC confinement, two overlapping VM lifetimes, and the 131,072-token context tier. `pnpm verify` passed 94 unit files with 415 tests passed and one declared skip, two native tests, TypeScript, lint, source limits, Rust and native helper checks, sidecar/resource validation, and the desktop production build.
- Core now keeps captured deliverables until an authoritative workspace delta changes or removes their paths, rejects bare interactive Python or Node shell commands before VM execution, keeps shell misuse recovery source-only until one source execution succeeds, and labels planning retries as revised plans. stdout, stderr, exit status, and unrelated programs do not change artifact freshness. This is macOS evidence only.

### Context turnover and oversized result cycle — 2026-08-07

- Before the fix, the real Gemma/no-NIC context case found the hidden exact total but failed `Expected automatic context compaction evidence.` with `contextCompactions=0`; its 406,996-byte stdout was only head/tail elided. The oversized table case consumed all six executions, claimed `/workspace/revenue_results.txt`, declared no artifact, and verified zero rows. Reports: `small-2026-08-06T15-36-20.066Z.json` and `small-2026-08-06T15-38-58.605Z.json`.
- Final focused context compaction passed in one execution with exact `COMPACTION_TOTAL=38687`, a 394,996-byte durable observation, and one traced inspectable compaction ledger. Report: `small-2026-08-07T04-14-13.705Z.json`.
- Final focused oversized result delivery passed in six executions. Gemma chose `revenue_received_summary.xlsx`; deterministic reopening with the pinned offline workbook libraries verified all 2,000 hidden `REVENUE_ROW_0001` through `REVENUE_ROW_2000` facts from real artifact bytes. Report: `small-2026-08-07T04-17-23.324Z.json`.
- The final default small sweep passed all seven sequential and three concurrent results with `maximumRunning=3`. It repeated both new cases successfully, plus XLSX row filtering, invalid-PDF clean stop, Romanian workbook routing, direct response, and terminal evidence translation. Report: `small-2026-08-07T04-42-36.032Z.json`.
- Focused management reports remained directly runnable and passed deterministic byte verification against the same four hidden facts: PDF in three executions (`small-2026-08-06T20-10-45.660Z.json`), DOCX in three (`small-2026-08-06T17-51-38.950Z.json`), XLSX in four (`small-2026-08-06T17-59-13.017Z.json`), and large-corpus XLSX in four (`small-2026-08-06T18-01-37.189Z.json`).
- Corrected scaled sequential completed as `scaled_stress_limit_found`: PDF produced `scaled-policy-report.pdf` but missed hidden `POLICY_PAGES=100`; 50-workbook XLSX completed six executions without a deliverable; 50-file cross-format stopped with `agent_stalled_duplicate`. Report: `scaled-sequential-2026-08-07T06-03-47.586Z.json`. The earlier completed concurrent run retained `maximumRunning=3` but all three report cases failed artifact verification (`scaled-concurrent-2026-08-07T05-31-03.264Z.json`). A corrected concurrent rerun was stopped after repeated model stalls and an `IO_ENOSPC` execution; it produced no qualifying report.
- The canonical physical `pnpm test:m3:macos` gate returned `certified` with real Python and Node artifacts, persistence, cancellation, resource limits, no-NIC confinement, two overlapping VM lifetimes, and the 131,072-token context tier. `pnpm verify` passed 94 unit files with 424 tests passed and one declared skip, two native tests, TypeScript, lint, source limits, Rust and native helper checks, sidecar/resource validation, and the desktop production build.

Focused XLSX row-filter regression validation on 2026-08-06 used the pinned Gemma worker, current-user daemon, and real no-NIC guest on physical Apple silicon. The fixture contained exactly 10 uppercase-extension XLSX workbooks under 10 subfolders, each with exactly 100 rows including the header. One hidden row per workbook contained `avans`; expected row markers remained outside the model prompt. The user task was the original terse request: `Generate an excel with all rows in excel files containing “avans”`.

- Before the fix, the agent reproduced the original sequence: unnecessary import failure, incorrect workbook-level dimension recovery, nested row serialization failures, repeated repairs, and all six executions consumed. The deterministic artifact check confirmed that the final workbook eventually contained all 10 records and no nonmatching rows, but the case correctly failed with `Expected at most 5 executions.` Report: `small-2026-08-06T08-50-49.525Z.json`.
- After adding explicit worksheet-dimension, flat-scalar row serialization, and malformed-Python recovery guidance to the XLSX skill, the identical case passed in two executions and 93.181 seconds. `avans_rows.xlsx` contained all 10 hidden records, no nonmatching `ordinary transfer` row, and the terminal execution reported clean `10/10` complete progress. Report: `small-2026-08-06T08-48-41.857Z.json`.
- No runtime execution, time, memory, context, or isolation limit changed. The focused stress oracle requires completion within five of the six available executions so capacity-exhaustion regressions fail. Focused prompt, loop, stress-profile, reporting, and TypeScript checks passed before the final physical run. This is macOS evidence only.

Focused generic-agent regression validation on 2026-08-06 ran on physical Apple silicon with the pinned Gemma worker and real no-NIC guest. The canonical `pnpm test:m3:macos` gate returned `certified`: real Python and Node tasks each completed with two executions and one artifact, while guest rehydration, repair, persistence, cancellation, timeout, output, security, workspace lifecycle, and two overlapping VMs passed at the 131,072-token hardware tier.

| Focused case | Final result | Exact evidence | Retained ignored reports and intermediate limits |
|---|---|---|---|
| Invalid document | **PASS** | One Python execution; real `EOF marker not found` evidence; exit 0; no artifact or retry. | The first run correctly identified the invalid PDF but used a separate discovery execution and failed `Expected at most 1 executions.` (`small-2026-08-05T21-53-47.305Z.json`). The corrected identical case passed in `small-2026-08-05T21-56-05.895Z.json`. |
| Romanian workbook routing | **PASS** | Unicode routing activated the workbook skill; complete clean progress; exact hidden `TOTAL_AVANS=12009`; three executions. | The first two runs found the exact total but ended `structured_tool_call_required` after concatenated markers or non-error stderr (`small-2026-08-05T21-56-56.638Z.json`, `small-2026-08-05T22-01-50.563Z.json`). A clean-evidence run then exposed ambiguous fixture column wording and returned `TOTAL_AVANS=0.0` (`small-2026-08-05T22-03-49.820Z.json`). After requiring separated markers, empty successful stderr, and explicit input column names, the identical case passed in `small-2026-08-05T22-05-56.358Z.json`. |

These runs validate only the canonical macOS gate and the two named focused stress cases. The complete small suite, scaled suite, and Windows certification were not run against this refactor.

The generic-agent refactor validation on 2026-08-05 used the new artifact-first `pdf-report` case on physical Apple silicon with the pinned Gemma worker and real no-NIC guest. The evaluator generated 11 realistic mixed-format files, kept expected facts outside the prompts, materialized the declared PDF, and opened it in a separate verification session. The production run derived the correct current facts (`MATCHING_INVOICES=8`, `INVOICE_TOTAL=20012.00`, `MEETING_NOTES=24`, `POLICY_PAGES=12`) and declared `management-report.pdf`, but independent extraction found unrelated fabricated metrics such as `REVENUE=450M`, `NET_INCOME=140M`, and `EMPLOYEE_COUNT=1200` in the artifact. The case therefore **failed** as `small_stress_limit_found`; response text and a declared artifact did not override artifact-byte evidence. The retained ignored report is `packages/eval/.generated/stress/small-2026-08-05T20-39-06.161Z.json`.

This physical run also exposed and drove generic fixes before the retained result: format-neutral progress could no longer auto-finish a multi-format deliverable task, caught workbook errors could not prove complete progress, and the then-current heuristic treated a deliverable as stale after later calculations unless recreated or explicitly verified. The 2026-08-07 delta-authoritative freshness change above supersedes that heuristic. The final retained failure is model output quality, not a passing product claim. The complete small suite, scaled suite, and Windows certification remain required before certification; the canonical macOS gate was subsequently rerun as recorded above.

The latest validation cycle was performed on 2026-08-03 on physical Apple silicon against `m3-stress-reliability` after the three reliability changes. This is macOS evidence only; it does not establish current Windows behavior.

The realistic fixture contained 10 files: TypeScript, JSON, CSV, Markdown, two XLSX workbooks with 10,000 rows, two DOCX files with 16 pages, and an eight-page PDF. Five sessions were started together.

| Suite or check | Result | Evidence | Failure or limit |
|---|---|---|---|
| Small stress suite | **PASS** | All 8 sequential and concurrent results passed twice consecutively. | None. |
| Sequential PDF | **PASS** | `PDF_PAGES=12`; `PDF_CHECKSUM=1326`. | None. |
| Sequential workbook | **PASS** | `XLSX_MATCHES=2`; `XLSX_TOTAL=2003`. | None. |
| Sequential XLSX folder | **PASS** | `XLSX_MATCHES=6`; `XLSX_TOTAL=12009`; one execution in both final runs. | None. |
| Sequential mixed folder | **PASS** | Exact XLSX and DOCX counts and checksums. | None. |
| Invalid PDF | **PASS** | Printed `INVALID_DOCUMENT_STOP=1` in one successful execution with no repair or artifact. | None. |
| Three concurrent cases | **PASS** | PDF, XLSX-folder, and mixed-folder cases passed with `maximumRunning: 3`. | None. |
| Realistic skill suite | **PASS** | All 5 cases passed twice consecutively across five simultaneously started sessions; audit valid. | None. |
| Direct response; no skill | **PASS** | Returned a correct concise response with zero executions. | None. |
| Terminal-only source discovery | **PASS** | Located `/source/src/pricing-rules.ts` and returned `SURCHARGE_BPS=275` without guessed paths or duplicate stall. | None. |
| Hidden-answer XLSX analysis | **PASS** | `XLSX_MATCHES=4`; `XLSX_TOTAL=6006.0`. | None. |
| Attached-PDF analysis | **PASS** | `PDF_PAGES=8`; `PDF_CHECKSUM=612`. | None. |
| Combined terminal and XLSX | **PASS** | Returned exact source evidence, `XLSX_MATCHES=4`, `XLSX_TOTAL=6006`, and `q2-audit.md` in both final runs. | None. |
| Dynamic skill disclosure | **PASS** | Direct: none; terminal: `terminal-commands`; workbook: `xlsx-workbooks`; PDF: `pdf-reading`; combined: terminal plus XLSX. Unselected skill bodies remained unloaded. | None. |
| Model and audit | **PASS** | Audit chain verified; model moved from `unloaded` to `ready`. | None. |
| Scaled sequential | **PASS** | 100-page PDF, one-million-row workbook, 50 workbooks, and 50-file mixed workflow returned exact results. | Large workbook workflows required resumable recovery and exceeded the intended 75-second work window in individual executions. |
| Scaled concurrent | **PASS** | Workbook, 50-workbook, and mixed workflows passed with `maximumRunning: 3`. | Large workflows used four to six executions and long-running batches. |
| Canonical `pnpm test:m3:macos` gate | **CERTIFIED** | Real Python and Node artifacts, persistence, cancellation, limits, live read-only source, confinement, cleanup, two overlapping VM lifetimes, and the 131,072-token cap passed. | macOS evidence only. |
| `pnpm verify` | **PASS** | Source limits, lint, type checking, 335 unit tests with one skip, two native tests, native helpers, desktop builds, and Rust checks passed. | None. |

The backend, daemon, model, guest isolation, audit, and canonical gate remained healthy. All four original reliability failures now pass. The final scaled suites also passed, while their long resumable executions remain reported separately rather than treated as proof of tighter work-window behavior.

## Reliability fix validation

### Collapsed workbook and result-table cycle — 2026-08-05

- A task-specific physical Apple-silicon reproduction used the real pinned Gemma worker, current-user daemon, and no-NIC guest against the original 36-workbook corpus. This is macOS evidence only; source paths and row contents remain in ignored local evidence.
- The generation task processed all 36 workbooks, found 17 matches, created exactly one 5,370-byte XLSX with worksheet dimension `A1:B18`, returned `Total matches found: 17`, and preserved a valid audit chain.
- The immediate table follow-up succeeded in one clean Python execution with zero stderr, returned exactly 17 GFM data rows, and reused the already verified complete corpus coverage. Core normalized overflow Markdown delimiters into the final cell instead of accepting malformed columns or requiring an unverified prose response.
- Non-qualifying iterations retained exact failures for collapsed `A1:A1` source metadata, `reset_dimensions()` on a normal worksheet, invalid table escape syntax, malformed GFM columns, omitted structured calls, duplicate repair, and an independently observed resource-contention timeout. No context, execution, or isolation limit was raised.
- Focused XLSX, prompt, output-contract, and combined source-recovery coverage passed: 44 tests across six files. `pnpm verify` passed with source limits, lint, TypeScript, 373 unit tests with one skip, two native tests, Rust checks, native helpers, sidecar validation, and the desktop build.

### Generated workbook delivery cycle — 2026-08-05

- A task-specific physical Apple-silicon reproduction used the real pinned Gemma worker, current-user daemon, and no-NIC guest against a 36-workbook source corpus. This is macOS evidence only.
- Before the fix, the guest processed all 36 workbooks and created the requested XLSX, but final response validation replaced the valid artifact-only completion with an empty response and ended the run with `agent_run_failed`.
- After the fix, the identical task succeeded in 82.323 seconds with an accepted final response, exactly one 5,369-byte XLSX deliverable, no checkpoint artifact, and a valid audit chain.
- Focused output-contract, artifact-declaration, and store coverage passed: 16 tests across four files. The checks cover preservation of generated-workbook final responses and exclusion of internal checkpoint files from current and historical user-visible artifact listings.
- `pnpm verify` passed with 366 unit tests, one skip, two native tests, TypeScript checks, Rust checks, native helpers, sidecar packaging, source limits, and the desktop build.

### Source discovery recovery cycle — 2026-08-03

- Focused loop and prompt tests passed: 16 tests across `loop-shell-recovery.test.ts` and `prompt-library.test.ts`.
- `pnpm verify` passed with 331 unit tests, one skip, two native tests, desktop builds, native helpers, and Rust checks.
- The realistic skill runner was repeated twice. Terminal-only passed both times. Combined source discovery also passed both times with `SOURCE_FILE=/source/src/pricing-rules.ts` and `SURCHARGE_BPS=275`, then reached workbook analysis without a guessed path or duplicate stall. The still-expected combined XLSX corpus/total limit remained for the aggregate-guidance commit. Reports: `packages/eval/.generated/stress/realistic-skills-2026-08-03T14-46-03.506Z.json` and `packages/eval/.generated/stress/realistic-skills-2026-08-03T14-50-38.346Z.json`.
- The small suite preserved all previous passes, including all three concurrent cases with `maximumRunning=3`. Invalid PDF and sequential XLSX-folder totals remained the planned later failures. Report: `packages/eval/.generated/stress/small-2026-08-03T14-55-01.388Z.json`.
- Scaled sequential and concurrent runs preserved the separately reported XLSX-folder aggregate limit; the one-million-row workbook and mixed 50-file workflow passed in both modes. Reports: `packages/eval/.generated/stress/scaled-sequential-2026-08-03T15-01-11.980Z.json` and `packages/eval/.generated/stress/scaled-concurrent-2026-08-03T15-16-33.806Z.json`.
- `pnpm test:m3:macos` remained certified with real Python and Node artifacts, persistence, cancellation, limits, no-NIC confinement, cleanup, two overlapping VM lifetimes, and the 131,072-token context cap. This command emits terminal evidence rather than a JSON report.

### Invalid PDF clean-stop cycle — 2026-08-03

- Focused PDF routing and attachment prompt tests passed: 14 tests across `prompt-library.test.ts` and `loop-attachments.test.ts`.
- `pnpm verify` passed with 332 unit tests, one skip, two native tests, desktop builds, native helpers, and Rust checks.
- The realistic skill suite preserved the source-discovery fixes, PDF and XLSX-only passes, skill routing, and audit validity. Combined XLSX totals remained the planned aggregate-guidance failure. Report: `packages/eval/.generated/stress/realistic-skills-2026-08-03T15-31-01.178Z.json`.
- The small suite was repeated twice. Invalid PDF passed both times with exactly one execution, `INVALID_DOCUMENT_STOP=1`, exit code 0, no repair, and no artifacts. `pypdf` emitted the bounded `EOF marker not found` parser diagnostic before the caught exception. The sequential XLSX-folder total remained the only small-suite failure; all previous and concurrent cases passed with `maximumRunning=3`. Reports: `packages/eval/.generated/stress/small-2026-08-03T15-36-30.587Z.json` and `packages/eval/.generated/stress/small-2026-08-03T15-42-36.222Z.json`.
- Scaled sequential preserved the XLSX-folder aggregate limit while the one-million-row workbook and mixed workflow passed. Report: `packages/eval/.generated/stress/scaled-sequential-2026-08-03T15-48-39.833Z.json`.
- The first scaled-concurrent run had a stochastic one-workbook total miss while the 50-file XLSX and mixed cases passed; the identical repeat restored the previous profile with workbook and mixed passes and only the known XLSX-folder aggregate limit. Reports: `packages/eval/.generated/stress/scaled-concurrent-2026-08-03T16-03-27.691Z.json` and `packages/eval/.generated/stress/scaled-concurrent-2026-08-03T16-13-52.846Z.json`.
- `pnpm test:m3:macos` remained certified with real Python and Node artifacts, persistence, cancellation, limits, no-NIC confinement, cleanup, two overlapping VM lifetimes, and the 131,072-token context cap. This command emits terminal evidence rather than a JSON report.

### Workbook aggregate and combined-workflow cycle — 2026-08-03

- Focused XLSX, source-recovery, rejection, and prompt tests passed: 28 tests across `loop-xlsx.test.ts`, `loop-shell-recovery.test.ts`, `loop-source-recovery.test.ts`, and `prompt-library.test.ts`.
- `pnpm verify` passed with 335 unit tests, one skip, two native tests, desktop builds, native helpers, and Rust checks.
- The final realistic skill suite passed twice consecutively. Terminal-only and combined source discovery returned `/source/src/pricing-rules.ts` and `SURCHARGE_BPS=275`; XLSX-only and combined processing used the complete case-insensitive corpus; combined returned `XLSX_MATCHES=4`, `XLSX_TOTAL=6006`, and `q2-audit.md`. Both reports retained `maximumRunning=5`, correct skill routing, and a valid audit chain. Reports: `packages/eval/.generated/stress/realistic-skills-2026-08-03T17-29-39.052Z.json` and `packages/eval/.generated/stress/realistic-skills-2026-08-03T18-15-31.820Z.json`.
- The final small suite passed twice consecutively. Sequential and concurrent XLSX-folder cases both returned `XLSX_MATCHES=6` and `XLSX_TOTAL=12009` in one execution; the one-workbook and mixed totals remained correct; invalid PDF still stopped cleanly in one execution; concurrent runs retained `maximumRunning=3`. Reports: `packages/eval/.generated/stress/small-2026-08-03T17-34-49.093Z.json` and `packages/eval/.generated/stress/small-2026-08-03T18-20-51.953Z.json`.
- Scaled sequential passed the 100-page PDF, one-million-row workbook, 50-workbook, and mixed 50-file workflows. The workbook folder returned `XLSX_MATCHES=500` and `XLSX_TOTAL=12752750`; mixed returned `XLSX_MATCHES=200`, `XLSX_TOTAL=2101100`, `WORD_PAGES=3000`, and `WORD_CHECKSUM=46651500`. Report: `packages/eval/.generated/stress/scaled-sequential-2026-08-03T17-39-20.658Z.json`.
- Scaled concurrent passed the one-million-row workbook, 50-workbook, and mixed workflows with `maximumRunning=3` and the same exact totals. The 50-workbook run used four executions and the mixed run used all six; long batches still exceeded the intended 75-second work window. Report: `packages/eval/.generated/stress/scaled-concurrent-2026-08-03T18-00-08.345Z.json`.
- The non-qualifying iterations exposed and retained exact evidence for a case-sensitive uppercase-XLSX miss, overcomplicated small-corpus checkpoint repair, guessed source extension allowlists, and an overlong combined recovery. Reports: `packages/eval/.generated/stress/realistic-skills-2026-08-03T16-30-56.384Z.json`, `packages/eval/.generated/stress/small-2026-08-03T16-45-16.946Z.json`, `packages/eval/.generated/stress/realistic-skills-2026-08-03T17-01-57.566Z.json`, `packages/eval/.generated/stress/realistic-skills-2026-08-03T17-11-59.149Z.json`, and `packages/eval/.generated/stress/realistic-skills-2026-08-03T17-18-34.096Z.json`.
- `pnpm test:m3:macos` remained certified with real Python and Node artifacts, persistence, cancellation, limits, no-NIC confinement, cleanup, two overlapping VM lifetimes, and the 131,072-token context cap. This command emits terminal evidence rather than a JSON report.
